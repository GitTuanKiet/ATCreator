import path from 'path';

const defaultDownloadPath = path.join('public', 'temp');
const defaultSoundPath = path.join('public', 'sounds');
const defaultOutputPath = path.join('public', 'videos');


export { defaultDownloadPath, defaultOutputPath, defaultSoundPath };
